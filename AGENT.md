# Solution Engineer Agent 项目说明

## 协同文档入口

阶段任务开始时，先阅读 [PROJECT_BRIEF.md](./PROJECT_BRIEF.md) 的协同与读取规则、[STATUS.md](./STATUS.md) 的当前进度、[DECISIONS.md](./DECISIONS.md) 的决策状态及 [ARCHITECTURE.md](./ARCHITECTURE.md) 的架构边界，再按阶段读取 [SPEC.md](./SPEC.md) 的详细契约。阶段交付需更新状态、验证证据和下一阶段输入。本文保留初始背景；细化的默认方案以 SPEC 及决策台账为准。

## 1. 项目定位

本项目用于构建一款面向 B2B 解决方案工程师、售前顾问和技术顾问的 AI 方案辅助工具。

产品需要将客户需求梳理、信息补全、行业与案例检索、解决方案生成、人工修订和结果导出串联为一条可追溯的工作流。它不是一个只依赖自由对话的通用聊天机器人。

项目暂定名称：`Solution Copilot`。

## 2. 核心目标

- 将会议纪要、招标文件和自然语言描述转换为结构化需求档案。
- 识别需求缺口、冲突和风险，并引导用户补充关键信息。
- 从内部知识库、历史案例和公开资料中检索可靠信息。
- 按标准模板分阶段生成专业、可编辑、可追溯的解决方案。
- 隔离不同组织、客户和项目的数据与记忆。
- 通过真实使用数据评估方案产出效率与质量。

## 3. 目标用户

- B2B 售前解决方案工程师
- 技术顾问与实施顾问
- 客户成功经理
- 产品方案经理

## 4. 核心业务流程

1. 创建客户和方案项目。
2. 输入或上传客户背景、会议纪要、原始需求等材料。
3. AI 提取结构化需求，并标记来源和置信度。
4. AI 识别缺失或冲突信息，提出澄清问题。
5. 检索内部产品资料、历史案例和必要的外部行业资料。
6. 先生成方案大纲，经用户确认后分章节生成内容。
7. 执行需求覆盖、事实引用、能力边界和数据泄漏检查。
8. 用户编辑、保存版本，并导出 Markdown、DOCX 或 PDF。
9. 将已确认结果沉淀为项目记忆或可复用案例。

## 5. 功能范围

### 5.1 MVP

- 客户与项目的创建、编辑、归档和状态管理。
- 文本、Markdown、PDF、DOCX 等需求材料的导入。
- 客户背景、痛点、目标、功能需求、非功能需求、约束、集成、合规和验收指标的结构化提取。
- 需求完整度评估、缺失信息追问和冲突提示。
- 内部知识库与案例库的文档解析、切分、索引和检索。
- 关键词与向量结合的混合检索。
- 带来源引用的方案大纲与章节生成。
- 会话记忆、项目记忆、客户记忆的分层管理。
- 会话和项目上下文的查看、删除与重置。
- 方案编辑、版本保存以及 Markdown、DOCX 导出。
- 基础运行记录，包括生成耗时、引用数量和人工修改次数。

MVP 的最小演示闭环：

> 上传客户会议纪要 → 提取并补全需求 → 检索相关资料与案例 → 生成带引用的完整方案 → 导出 Word 文档。

### 5.2 后续增强

- 互联网行业调研与网页内容提取。
- 检索结果重排与来源可信度评分。
- 方案质量自动评分和修改建议。
- 版本差异对比与历史版本恢复。
- 多用户协作、角色权限、审批和审计日志。
- PPT 大纲或演示文稿生成。
- 模板市场、多模型切换、企业 SSO。
- 使用效果与业务指标看板。

## 6. 关键产品原则

### 6.1 事实可追溯

- 行业数据、产品能力和案例描述应尽量附带来源。
- 明确区分内部资料、历史案例、公开资料和模型推断。
- 无可靠来源的数字和结论必须标记为待核实，不得伪装为事实。

### 6.2 人在回路

- 需求确认、方案大纲、重要假设和最终交付物应允许人工确认或修改。
- Agent 工作流需要支持暂停、恢复、重试和局部重新生成。

### 6.3 客户数据隔离

- 所有核心业务数据必须关联组织；客户私有数据还必须关联客户。
- 检索、记忆和生成过程不得混入其他客户的私有信息。
- 删除或重置记忆时，应明确操作范围，不得静默扩大删除范围。

### 6.4 结构化优先

- 重要需求与方案内容应保存为结构化数据，而不是只保存在对话文本中。
- 生成过程优先采用“提取 → 澄清 → 检索 → 大纲 → 分章生成 → 校验”的显式工作流。

### 6.5 指标必须可验证

- 不在产品中预设“效率提升 60%”等结果。
- 效率与质量结论应由生成耗时、修改次数、采用率、用户评分等真实数据计算得到。

## 7. 当前技术方向

以下是当前建议，并非不可变更的最终技术约束：

| 层级 | 技术方向 | 主要用途 |
| --- | --- | --- |
| Web 前端 | Next.js、React、TypeScript | 项目工作台、对话与方案编辑 |
| UI | Tailwind CSS、shadcn/ui | 后台界面与通用组件 |
| 数据请求/状态 | TanStack Query、Zustand | 服务端数据同步与本地交互状态 |
| 富文本编辑 | Tiptap 或 Lexical | 方案编辑与段落级 AI 操作 |
| 后端 API | Python、FastAPI | 业务接口、模型调用和文档处理 |
| Agent 编排 | LangGraph | 状态化工作流、人工确认、暂停与恢复 |
| 主数据库 | PostgreSQL | 用户、客户、项目、方案和工作流状态 |
| 向量检索 | pgvector | 文档与案例的语义检索 |
| 关键词检索 | PostgreSQL Full Text Search | 关键词检索与混合召回 |
| 缓存与任务队列 | Redis | 缓存、限流和异步任务 |
| 异步任务 | Celery 或 Dramatiq | 文档解析、索引、调研和导出 |
| 文件存储 | S3 兼容对象存储或 MinIO | 原始文档与导出文件 |
| 文档处理 | PyMuPDF、python-docx | PDF/DOCX 解析与 DOCX 生成 |
| 可观测性 | OpenTelemetry；可选 LangSmith | 调用链路、耗时、错误与评估 |
| 部署 | Docker Compose 起步 | 本地开发和演示环境 |

模型调用层应通过统一适配接口封装，避免将业务逻辑绑定到单一模型或供应商。

## 8. 初始领域实体

- 用户与组织：`User`、`Organization`、`Membership`
- 客户与项目：`Customer`、`Project`
- 需求：`RequirementProfile`、`RequirementItem`、`Clarification`
- 对话与记忆：`Conversation`、`Message`、`Memory`
- 知识：`Document`、`DocumentChunk`、`ResearchSource`
- 方案：`SolutionTemplate`、`Solution`、`SolutionVersion`、`Citation`
- 运行与评估：`GenerationRun`、`EvaluationResult`

实体字段、关系、索引和生命周期需要在数据库设计阶段另行确定。

## 9. 当前非目标

- 不以构建完全自主、无需人工确认的销售 Agent 为目标。
- MVP 不包含自动报价、自动签约或直接向客户发送方案。
- MVP 不追求覆盖所有行业，优先选择一个示例行业形成高质量闭环。
- 不将模型生成内容默认视为经过验证的事实。
- 不为了简历数据而伪造用户量、产量或效率提升指标。

## 10. 待后续维护的规范

以下内容当前不在本文件中定稿，待项目进入实现阶段后补充：

- 仓库目录结构和模块边界
- 编程语言与框架的具体版本
- 代码风格、命名和注释规范
- API、错误码和日志规范
- 数据库迁移与种子数据规范
- Prompt、工具和 Agent 节点开发规范
- 测试策略、覆盖率和验收标准
- 安全、隐私和密钥管理规范
- Git 分支、提交、评审和发布流程
- 本地开发、CI/CD 和部署操作规范

在这些规范补充前，实施者应保持改动范围清晰、避免过早抽象，并优先保证 MVP 业务闭环可验证。
