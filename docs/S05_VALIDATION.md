# S05 验收记录

日期：2026-09-09；输入 `d93860f`，main，当前任务，ponytail full。状态：工作区验收通过，review，尚未提交。

## 已完成检查

- 新迁移 `c26548e562eb` 已审查并应用，包含会话、消息、运行、澄清、记忆与审计表。
- PostgreSQL 隔离 schema 完整回归：`RUN_DB_TESTS=1 uv run --group test pytest -q --tb=short`，31 passed。
- S05 三组集成测试覆盖：跨组织/只读拒绝、请求幂等、重复 Worker 执行、消息引用、SSE 游标重放、澄清去重/回答回写/必答禁止跳过、记忆版本与作用域、会话/项目重置、旧结果取消、权限撤销、租约补偿、人工档案编辑拒绝迟到发布。
- OpenAPI/TypeScript 生成、Ruff、Prettier、`pnpm check` 和生产构建通过；Alembic check 无差异。最终重置/来源修正后 S05 三项再次通过，其余 28 项未修改。
- 真实 Web→API→dispatcher→Worker→DeepSeek：合成输入生成 2 项需求，含排队 33.78 秒；断开首次流后携带 Last-Event-ID，重放 5 个后续事件，无重复开始事件。直接澄清回答写入需求，确认客户记忆后重置会话，该记忆仍保留。产物 `evals/s05-results.json`。
- 浏览器：键盘提交验收指标、建议问题跳过、项目记忆保存/确认/来源展开、需求来源跳回对应会话消息均通过。320/768/1024/1440 无横向溢出，单 h1，控制台无 warn/error；修复了通用 li 横向样式影响窄屏消息的问题。
- 保留演示项目 `9524b6e0-a7d9-4d59-8767-962861effe41`，会话 `a5c2b6f5-1fb3-4c71-8a0b-46faa251984c`。评测 JSON 记录浏览器人工操作前的快照。

## 实施边界

- SSE 先持久化开始事件，模型 JSON 完整校验后发布文本分段、来源和终态；不是 provider token streaming。
- waiting_user 的后续输入建立新运行，S06 再引入 LangGraph 检查点。
- 澄清为确定性缺口模板，回答仍需人工确认；对话模型仅从最新用户消息提取有逐字引用的候选项。
- 记忆显式保存并确认，不自动把模型推断写入客户长期记忆。重置不物理删除历史消息，也不清空需求档案。
- 现有 PyMuPDF / Starlette 测试依赖弃用警告保留；生产 OIDC、部署、长上下文质量与完整屏幕阅读器验收不属于本次已验证范围。
- 用户开始前已有 README 空行及 package.json / pnpm-lock.yaml 包管理器升级修改，保留，S05 不引入新依赖。

## 已知质量偏差

真实模型将“客户自有服务器部署”扩写为“不能使用云服务等外部托管方式”，且将“100万元”补充为“人民币”；原文引用虽正确，概括的语义仍可能超出原文。因此提取保持 proposed，需人工修正后确认，不把逐字引用校验等同于内容完全正确。S06/S09 继续扩充真实脱敏样例。

首次冒烟假设 25 秒内一定获得事件，在排队时触发断言；脚本已改为正常周期重连后通过。SSE 未做整机网络故障或反向代理集群测试，完整 axe/屏幕阅读器审计未执行。
