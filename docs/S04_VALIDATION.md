# S04 需求结构化验收

日期：2026-09-09；输入 `ebe5f68`；实现整合 `00cdedc`，main。S04 本地阶段验收通过。

## 交付

新增项目需求档案和持久化提取记录，迁移 `1e32ee8d72cd`。需求项以稳定 UUID 保存在档案 JSONB，项目锁和 version 保证原子更新；完整度按八个必备类别计算。接口支持文本/已解析资料提取、手工新增/编辑、确认/拒绝、摘要编辑、冲突保留/替换、整档确认、任务取消与刷新恢复。具体简化边界见 D016。

DeepSeek JSON 输出经 Pydantic 校验；原文引用必须在选定来源中逐字匹配。输入不接受模型提供的租户范围；选取资料、Worker 开始和结果发布均重新检查授权及来源 generation。已有人工值不会被模型覆盖，版本竞争直接拒绝发布。最小摘录与章节/页码/行号作为来源快照保留，下载仍实时鉴权。

## 验证命令与结果

- `RUN_DB_TESTS=1 uv run --group test pytest -q`：28 passed，含 S00–S04 回归。
- `uv run ruff check apps backend scripts tests`、Ruff format、前端 Prettier：通过。
- `uv run alembic check`：No new upgrade operations detected。
- OpenAPI 导出与 TypeScript 契约生成、`next build`：通过。
- `uv run --group test python -m scripts.smoke_s04`：真实 API→dispatcher→Worker→DeepSeek→PostgreSQL 通过；结果保存 `evals/s04-results.json`。

集成测试覆盖跨组织访问、只读角色、客户/项目归档、另一客户资料被拒、文档来源定位、来源重建拒绝发布、引用伪造拒绝、重复执行、确认值保留、数值冲突、显式解决、版本过期、模型调用期间人工编辑、撤销权限、取消与租约恢复。测试模型替身只用于确定性错误路径，不能替代真实调用。

## 真实模型结果

模型为 `.env` 配置的 `deepseek-v4-flash-vision-exp`，提示版本 `s04-v1`。只发送合成材料；最终一次提取 7.24 秒生成 9 项，引用逐字匹配且确认成功。八个必备类别命中七个，页面完整度为 87.5%。模型将“资料分散、查找困难”合入背景，没有单独分类为业务痛点；这不是材料不存在，用户可编辑分类或补充独立需求。该单样本不代表生产质量或性能指标。

初次调用因本机 Python 缺少默认 CA 文件失败，已通过现有 certifi 信任集合修复；TLS 校验保持开启，未改系统代理或网络设置。供应商数据留存和企业合规策略尚未验证。

## 浏览器证据

- 合成项目打开后展示摘要、分组需求、具体缺失类别、只读原文引用和提取状态。
- 对已确认的 100 万元预算输入新的 80 万元预算：原项保留、新项 conflicted，整档确认不可用。
- 显式“采用此项并拒绝原项”：旧预算变为 rejected，新预算 confirmed；引用展开可看到本次会议原文。
- 人工编辑表单绑定打开时的 version，后台刷新不能使旧输入取得新版本号；摘要编辑也保留独立草稿。
- 最终页面人工修改预算并保存成功，状态从已确认转为待确认，原来源保留；页面单 h1、控件具有可访问名称，桌面排版正常，控制台无 error/warn。最终演示项目为 `cbf96c50-8ac6-4b01-84ff-c6e75d3b7afa`；JSON 证据是人工编辑前的模型结果。

## 边界与交接

- 每次最多 10 份资料、30000 字/200 片段；既有档案传给模型的标题和内容合计最多 30000 字。每档最多 200 项，单项目仅一个活动提取。
- 单次调用超时 120 秒，Worker 租约 180 秒；失联最多恢复三次，模型失败由用户重新提取。迟到结果因状态/attempt/version 检查不会覆盖现有档案；不保证供应商调用恰好一次。
- 相似项关系由模型建议并做数值/否定守卫，复杂语义冲突仍需人工核对。不得把置信度或完整度当事实正确率。
- S05 接入消息来源和澄清回答；S06 再做 LangGraph；S07 扩展方案版本引用。暂未提供跨项目需求分析、完整历史版本浏览和生产部署。
- 本次完成桌面浏览器验收；新需求页面的四断点、屏幕阅读器和完整 axe 审计尚未执行，不沿用 S02 的测试结果宣称通过。
