# Solution Copilot 决策台账

更新日期：2026-09-06。

## 状态与维护方法

- **accepted**：用户明确要求或已有明确确认；记录来源。
- **baseline**：现有 SPEC 的默认实现方案，尚未完成兼容性/运行验证，不代表用户逐项确认。
- **proposed**：建议方案，尚未选定。
- **open**：信息不足，需在指定阶段解决。
- **superseded**：被新决定替换，保留原记录并链接新 ID。

新增决定使用递增 ID，记录日期、原因、影响、依据和验收方法。重大范围或外部资源变更交由用户确认；授权范围内常规实现选择由执行任务说明理由后推进，不逐项增加审批。不能将 proposed/open 自动升级成 accepted。

## D001 — 总控与阶段任务使用共享文档交接

- 状态：accepted；日期：2026-09-05。
- 决定：使用 PROJECT_BRIEF、ARCHITECTURE、STATUS、DECISIONS 作为跨阶段资料，结合现有 AGENT 和 SPEC；总控规划与验收，阶段任务执行。
- 原因：避免关键事实只保存在长聊天中。
- 依据：用户当前明确要求，参考讨论“项目任务规划方式”。
- 影响：每个阶段开始读取资料，交付时登记测试、决策和下一阶段输入。
- 验收：下阶段可从文档和代码版本独立恢复上下文；本决定不代表已经创建任务或配置应用内项目。

## D002 — 保留既有说明和规格的职责

- 状态：accepted；日期：2026-09-05。
- 决定：保留用户指定的 AGENT.md；详细实现及依赖继续维护在 SPEC.md。新增四份文档负责目标、架构摘要、进度、决策，避免复制全部字段和包清单。
- 依据：用户先前关于 AGENT、代码 SPEC、依赖维护的要求及本次四文件要求。
- 影响：AGENT 增加文档入口；代码风格和具体团队工程规范仍后续维护。
- 验收：文件分工明确，技术变更能定位到唯一详细规格。

## D003 — 首先完成需求到方案导出的 MVP

- 状态：baseline；日期：2026-09-05。
- 决定：当前基线交付结构化需求、内部检索、人工确认、带引用方案、版本及 DOCX/Markdown 导出。
- 原因：提供可以直接演示和评估的业务闭环。
- 依据：AGENT 第 5 节、SPEC 第 2–3、22 节。
- 影响：互联网调研、高级重排、PPT 等增强功能不阻碍首次交付；不承诺简历中的量化成果。

## D004 — Web/API/Worker 共享模块化业务层

- 状态：baseline；日期：2026-09-05。
- 决定：Next.js + FastAPI + Celery，Python 共享领域和应用服务；Tiptap 编辑器、LangGraph 编排。
- 原因：先独立验证功能，再组合 Agent；UI、HTTP 和队列不拥有独立版本的业务规则。
- 依据：SPEC 第 4–5、10、14 节；优先采用其中已明确的 Tiptap/Celery，AGENT 早期备选保留为历史背景。
- 影响：S00 建骨架，S06 编排，S07 接入编辑和完整生成。测试与实际依赖解析后可修订。

## D005 — PostgreSQL/pgvector 与对象存储

- 状态：baseline；日期：2026-09-05。
- 决定：PostgreSQL 保存业务/检查点/检索索引，Redis 处理队列和缓存，S3-compatible 保存文件，本地计划 Compose + MinIO。
- 原因：让业务数据与检索元数据集中管理，并分离长任务和大文件。
- 依据：SPEC 第 4、7–9、17 节。
- 影响：SQLite 不能替代关键集成测试；部署、镜像可用性和安装状态尚未验证。

## D006 — 包清单与锁文件分工

- 状态：baseline；日期：2026-09-05。
- 决定：pnpm 管前端、uv 管 Python；初始化时生成真实 manifest 与锁文件；只按模块需要安装依赖。
- 原因：规格记录用途，锁文件记录可复现版本。
- 依据：SPEC 第 4.2–4.9 节。
- 影响：当前版本范围是建议基线，S00 必须验证包兼容性、镜像、许可证与可获取性。未解析前不得标记依赖“已验证”。

## D007 — 阶段执行编号统一为 S00–S09

- 状态：baseline；日期：2026-09-05。
- 决定：按 STATUS 的十个执行阶段交接，映射 SPEC 原五个高层阶段。
- 原因：保留既有规格，同时让任务边界足够小且可验收。
- 影响：所有新交接记录使用 Sxx，测试随阶段开展；默认顺序推进，同目录避免并发写入冲突。
- 验收：每阶段具备输入、产出、负责人、证据与下一阶段输入。

## D008 — 身份与数据授权细化

- 状态：open；解决期限：S01，责任：阶段执行者/总控。
- 待定：OIDC 提供方、开发身份机制、member/viewer 的客户授权映射、是否启用 PostgreSQL RLS。
- 原因：SPEC 定义了角色但没有完整资源授权表；接口过滤必须有可靠授权来源。
- 验收：两组织、同组织两个客户、不同角色样例覆盖 API、Worker、检索及恢复路径。开发身份不可在生产开启。

## D009 — 模型、Embedding 与中文检索

- 状态：open；解决期限：S03（检索）、S04（生成），责任：阶段执行者；账号和业务约束由用户提供。
- 待定：模型供应商、可用凭据、数据处理设置、Embedding 维度与中文分词策略。
- 原因：SDK 名称不代表已选服务商；中文检索需要实际数据验证。
- 验收：中文评测结果、调用可用性、token/维度配置与索引一致；更换 Embedding 有重建策略。

## D010 — 公共知识、证据生命周期与下载

- 状态：open；解决期限：S02 知识作用域、S07 证据、S08 下载；责任：阶段执行者/总控。
- 待定：public 是系统发布还是跨组织共享；文档删除时历史引用保留方式；下载选择即时鉴权代理还是允许短暂有效的签名链接。
- 原因：SPEC 对组织字段与 public 作用域、删除与历史引用、签名下载与立即撤权存在需收敛的边界。
- 默认约束：普通用户不能发布跨组织公共资料；下载不得削弱 SPEC 撤权验收要求，采用签名链接须明确限制并调整经确认的验收契约。
- 验收：权限撤销、来源删除和旧版本导出场景有明确行为及测试。

## D011 — 演示行业、模板与 PDF

- 状态：open；解决期限：S04 前确定样例行业，S07 确定模板，S08 确定导出边界；责任：总控/用户。
- 待定：首个行业、资料来源、品牌样式、是否增加 PDF 导出。
- 当前基线：DOCX/Markdown 为 MVP，PDF 为可选；不把未确认增强项作为已承诺结果。
- 验收：脱敏样例覆盖正常、缺失及冲突需求；生成文档可打开且结构完整。

## D012 — 任务与事件的持久化契约

- 状态：open；解决期限：S02 任务入队、S05 SSE、S06 恢复；责任：阶段执行者。
- 待定：幂等键、入队失败补偿、Worker 失联恢复、事件存储与保留期、Last-Event-ID 重放及重复 resume 处理。
- 原因：SPEC 已要求可恢复行为，仍需具体存储和事务设计。
- 验收：入队失败不丢任务，重连不丢终态，重复执行不重复生成版本，取消有最终可查询状态。

## 新决策模板

```text
## Dxxx — 标题
状态 / 日期：
决定或待定问题：
依据与原因：
替代方案及取舍：
影响范围 / 解决期限 / 负责人：
验证证据：
替代的旧决定 / 被哪项决定替代：
```

## D013 — S00 最小可运行依赖与本地进程

- 状态：baseline；日期：2026-09-06；责任：当前任务。
- 决定：Node 24/Python 3.12、pnpm 10（packageManager 固定）、uv 0.8.19；Next.js 16/React 19，FastAPI/Celery 共享 Python 包。精确包版本只存锁文件。
- 原因：按 D006 仅引入当前代码使用的依赖，SQLAlchemy/Alembic、认证在 S01 接入，模型、解析、编辑器随后按阶段接入；S00 原生 CSS 足够支持状态页。
- 运行：Compose 管基础设施，开发脚本管理三个宿主进程；不在本阶段声称生产部署已验证。
- 健康：API 存活与就绪分开，Web 同源代理；真实 Worker 探针使用临时 RPC 结果回传。尚无业务任务提交端点，因此 D012 的持久化与入队补偿仍由 S02 落实。
- 初始化：显式幂等创建 vector 扩展与私有 bucket；不由 API 启动自动改表。
- 基线核对：[Next.js 16 运行要求](https://nextjs.org/docs/app/guides/upgrading/version-16)、[pnpm 10 安装](https://github.com/pnpm/pnpm.io/blob/main/versioned_docs/version-10.x/installation.md)、[MinIO 镜像与许可证](https://hub.docker.com/r/minio/minio/)。MinIO 固定已发布 release，仅本地开发；生产发布须重新验证维护与许可证条件。
- 验证证据与未验证项：见 STATUS S00 交接及 docs/S00_VALIDATION.md。
