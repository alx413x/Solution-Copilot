# Solution Copilot 决策台账

更新日期：2026-09-11。

## 状态与维护方法

- **accepted**：用户明确要求或已有明确确认；记录来源。
- **baseline**：现有 SPEC 的默认实现方案，尚未完成兼容性/运行验证，不代表用户逐项确认。
- **proposed**：建议方案，尚未选定。
- **open**：信息不足，需在指定阶段解决。
- **superseded**：被新决定替换，保留原记录并链接新 ID。

新增决定使用递增 ID，记录日期、原因、影响、依据和验收方法。重大范围或外部资源变更交由用户确认；授权范围内常规实现选择由执行任务说明理由后推进，不逐项增加审批。不能将 proposed/open 自动升级成 accepted。

## D001 — 总控与阶段任务使用共享文档交接

- 状态：accepted；日期：2026-09-05。
- 决定：使用 PROJECT_BRIEF、ARCHITECTURE、STATUS、DECISIONS 作为跨阶段资料，结合现有 AGENT 和 SPEC；总控规划与验收，阶段任务执行。
- 原因：避免关键事实只保存在长聊天中。
- 依据：用户当前明确要求，参考讨论“项目任务规划方式”。
- 影响：每个阶段开始读取资料，交付时登记测试、决策和下一阶段输入。
- 验收：下阶段可从文档和代码版本独立恢复上下文；本决定不代表已经创建任务或配置应用内项目。

## D002 — 保留既有说明和规格的职责

- 状态：accepted；日期：2026-09-05。
- 决定：保留用户指定的 AGENT.md；详细实现及依赖继续维护在 SPEC.md。新增四份文档负责目标、架构摘要、进度、决策，避免复制全部字段和包清单。
- 依据：用户先前关于 AGENT、代码 SPEC、依赖维护的要求及本次四文件要求。
- 影响：AGENT 增加文档入口；代码风格和具体团队工程规范仍后续维护。
- 验收：文件分工明确，技术变更能定位到唯一详细规格。

## D003 — 首先完成需求到方案导出的 MVP

- 状态：baseline；日期：2026-09-05。
- 决定：当前基线交付结构化需求、内部检索、人工确认、带引用方案、版本及 DOCX/Markdown 导出。
- 原因：提供可以直接演示和评估的业务闭环。
- 依据：AGENT 第 5 节、SPEC 第 2–3、22 节。
- 影响：互联网调研、高级重排、PPT 等增强功能不阻碍首次交付；不承诺简历中的量化成果。

## D004 — Web/API/Worker 共享模块化业务层

- 状态：baseline；日期：2026-09-05。
- 决定：Next.js + FastAPI + Celery，Python 共享领域和应用服务；Tiptap 编辑器、LangGraph 编排。
- 原因：先独立验证功能，再组合 Agent；UI、HTTP 和队列不拥有独立版本的业务规则。
- 依据：SPEC 第 4–5、10、14 节；优先采用其中已明确的 Tiptap/Celery，AGENT 早期备选保留为历史背景。
- 影响：S00 建骨架，S06 编排，S07 接入编辑和完整生成。测试与实际依赖解析后可修订。

## D005 — PostgreSQL/pgvector 与对象存储

- 状态：baseline；日期：2026-09-05。
- 决定：PostgreSQL 保存业务/检查点/检索索引，Redis 处理队列和缓存，S3-compatible 保存文件，本地计划 Compose + MinIO。
- 原因：让业务数据与检索元数据集中管理，并分离长任务和大文件。
- 依据：SPEC 第 4、7–9、17 节。
- 影响：SQLite 不能替代关键集成测试；部署、镜像可用性和安装状态尚未验证。

## D006 — 包清单与锁文件分工

- 状态：baseline；日期：2026-09-05。
- 决定：pnpm 管前端、uv 管 Python；初始化时生成真实 manifest 与锁文件；只按模块需要安装依赖。
- 原因：规格记录用途，锁文件记录可复现版本。
- 依据：SPEC 第 4.2–4.9 节。
- 影响：当前版本范围是建议基线，S00 必须验证包兼容性、镜像、许可证与可获取性。未解析前不得标记依赖“已验证”。

## D007 — 阶段执行编号统一为 S00–S09

- 状态：baseline；日期：2026-09-05。
- 决定：按 STATUS 的十个执行阶段交接，映射 SPEC 原五个高层阶段。
- 原因：保留既有规格，同时让任务边界足够小且可验收。
- 影响：所有新交接记录使用 Sxx，测试随阶段开展；默认顺序推进，同目录避免并发写入冲突。
- 验收：每阶段具备输入、产出、负责人、证据与下一阶段输入。

## D008 — 身份与数据授权细化

- 状态：open（部分落实）；开发身份、角色与客户授权、RLS 选择已由 D014 落实；真实生产 OIDC 供应商与联调仍待部署前确定。
- 待定：OIDC 提供方、开发身份机制、member/viewer 的客户授权映射、是否启用 PostgreSQL RLS。
- 原因：SPEC 定义了角色但没有完整资源授权表；接口过滤必须有可靠授权来源。
- 验收：两组织、同组织两个客户、不同角色样例覆盖 API、Worker、检索及恢复路径。开发身份不可在生产开启。

## D009 — 模型、Embedding 与中文检索

- 状态：open（S03/S04 本地调用已验收；生产数据策略待确认）；责任：阶段执行者；账号和业务约束由用户提供。
- S03 决定：使用本地 `BAAI/bge-small-zh-v1.5`，固定 revision，512 维；jieba 中文分词，tokenizer 分块 440、重叠 80。模型 profile 隔离旧向量，变更后重建索引。MVP 使用 PostgreSQL 精确向量扫描和全文检索，经 RRF 融合。
- S04：用户在被忽略的 `.env` 配置 DeepSeek，真实 JSON 提取、引用核验与冲突演示通过。合成单样本 7.24 秒/9 项，存在痛点分类偏差；记录模型 usage，费用核算、供应商留存与生产数据策略尚未验证。Embedding 继续使用独立本地模型。
- 原因：生成与向量化用途分开，文档可留在本机完成索引；中文检索需要实际数据验证。
- 验收：S03 的 30 条真实中文问题 Hit@8 100%、MRR@8 0.901、HTTP P95 23.65 ms；授权前置、引用定位、维度/profile 和重建行为由集成测试及浏览器验证。S04 单独验收生成调用。

## D016 — S04 需求档案与提取发布

- 状态：baseline（已实施并通过本地验收）；日期：2026-09-09；依据：用户要求执行 S04、SPEC 7.3/11/13.5 与 ponytail full。
- 需求项以带稳定 UUID 的 JSONB 数组保存在项目唯一的 requirement_profiles 中，最多 200 项；客户归属从 Project 联结获取。项目锁与档案 version 保证整个档案原子更新；需要跨项目逐项分析或更大档案时再拆 requirement_items 表。
- requirement_extractions 单独持久化输入快照、档案基础版本、状态、尝试次数、租约和模型/usage；复用现有 Celery 与 dispatcher，保留 S02 jobs 的文档外键。每项目只允许一个活动提取任务；进程失联最多恢复 3 次，模型/校验失败由用户重新提取，取消阻止迟到发布。
- DeepSeek 使用 JSON 输出，Pydantic 约束字段/长度/类别，引用 quote 必须逐字存在于服务端选定的来源。单次最多 30000 字、200 个片段、10 份资料；只读取组织、当前客户或当前项目内可访问资料。调用前及发布前重新鉴权，重建/停用/删除来源时拒绝发布。
- 模型只建议 duplicate/conflict 关系；合并不覆盖已有内容，数值或否定变化保守标为冲突，保留全部来源。人工修改与确认优先；提取期间档案版本变化则失败，要求重新提取。冲突通过保留原项或采用新项显式解决；未解决冲突禁止确认整个档案。
- 完整度按八个必备类别等权计算，同时返回缺失类别，不由模型打分；澄清回答留给 S05。引用保存最小原文摘录与定位快照，历史摘录随项目授权可读，原文件下载仍实时鉴权；S07 再定义方案版本引用。
- HTTPS 保持证书验证开启，使用已安装 certifi 的 CA 集合，修复本机 Python 缺少默认 CA 文件的问题；不更改系统网络或代理配置。

## D010 — 公共知识、证据生命周期与下载

- 状态：open（历史证据与 S08 下载已由 D019/D020 落实；public 仍未开放）；责任：阶段执行者/总控。
- 待定：public 是系统发布还是跨组织共享；文档删除时历史引用保留方式；下载选择即时鉴权代理还是允许短暂有效的签名链接。
- 原因：SPEC 对组织字段与 public 作用域、删除与历史引用、签名下载与立即撤权存在需收敛的边界。
- 默认约束：普通用户不能发布跨组织公共资料；下载不得削弱 SPEC 撤权验收要求，采用签名链接须明确限制并调整经确认的验收契约。
- 验收：权限撤销、来源删除和旧版本导出场景有明确行为及测试。
- S02 收敛：组织资料所有成员可读、仅 owner 可写；客户/项目资料复用客户授权与归档规则。public 暂不开放发布或跨组织读取。原文件使用每请求鉴权代理下载、no-store；删除立即隐藏，持久化清理原文件与 Chunk。历史方案引用快照仍待 S07/S08。
- S07 实施（待验收）：方案版本在章节 JSONB 内保存 quote、来源标识、文档/Chunk/generation 和定位快照，不以外键依赖可删除的 Chunk。历史 quote 按当前项目 ACL 可读；资料停用或删除后仍保留快照，但原文件下载继续实时鉴权并拒绝访问。S08 导出下载由 D020 收敛为同样的 API 鉴权代理。

## D011 — 演示行业、模板与 PDF

- 状态：open；解决期限：S04 前确定样例行业，S07 确定模板，S08 确定导出边界；责任：总控/用户。
- 待定：首个行业、资料来源、品牌样式、是否增加 PDF 导出。
- 当前基线：DOCX/Markdown 为 MVP，PDF 为可选；不把未确认增强项作为已承诺结果。
- S07 实施（待验收）：采用通用 B2B 售前/知识工作台合成样本和可编辑 14 章模板，不加入品牌或特定行业假设。DOCX 样式和 PDF 范围继续留给 S08。
- 验收：脱敏样例覆盖正常、缺失及冲突需求；生成文档可打开且结构完整。

## D012 — 任务与事件的持久化契约

- 状态：implemented（D015/D017/D018）；S02 任务入队、S05 SSE、S06 恢复已实施；责任：阶段执行者。
- 待定：幂等键、入队失败补偿、Worker 失联恢复、事件存储与保留期、Last-Event-ID 重放及重复 resume 处理。
- 原因：SPEC 已要求可恢复行为，仍需具体存储和事务设计。
- 验收：入队失败不丢任务，重连不丢终态，重复执行不重复生成版本，取消有最终可查询状态。
- S02 收敛：PostgreSQL jobs 同时作为 outbox，独立 dispatcher 每 2 秒补偿，成功投递后 30 秒未领取会重发；Redis 不保存权威终态。文档 ID + generation 为业务幂等键；文档锁后任务锁、attempt fencing、300 秒可续租租约，最多 3 次自动尝试。任务开始及解析检查点、发布前重新鉴权。取消后旧 attempt 无权提交。S05/S06 事件与工作流恢复由 D017/D018 收敛。

## D015 — S02 解析版本与限制

- 状态：accepted / 2026-09-07；当前任务执行。
- S02 终态为 `parsed`（解析完成）；`ready` 专用于 S03 索引完成。不创建未选模型的向量字段。Chunk 归属从 Document 联结取得，组合外键保证组织一致性。
- S02 使用可复现的 Unicode 字符切分（目标 700、重叠 100），优先结构边界，表格按行切分并重复表头；`token_count` 留空，`metadata.character_count` 保存字符数。S03 确定模型后添加真实 tokenizer 与 embedding 配置并评测重建，不能把字符数冒充模型 token。
- PDF 保存页码/文本块坐标；TXT/MD 保存行号，MD/DOCX 保存章节；DOCX 保存正文块序号，不伪造分页。扫描 PDF 无 OCR，密码 PDF、空文本和损坏文件给出操作建议。PDF 复杂表格当前按文本块提取，表格结构保真限 DOCX/Markdown。
- UTF-8 文本；25 MiB 上传上限可配置，DOCX 展开上限 100 MiB/2000 entries，PDF 最多 1000 页、提取总字符上限 200 万。解析器不执行宏或外部链接。
- 上传先保存 DB 意图，后写私有对象；中断/模糊 PUT 结果由任务读取并验证 SHA256。删除使用 tombstone 和 dispatcher 重试对象删除；同作用域重新上传可恢复同一文档 ID。S07 建引用前必须将解析版本替换扩展为保留引用快照。

## 新决策模板

```text
## Dxxx — 标题
状态 / 日期：
决定或待定问题：
依据与原因：
替代方案及取舍：
影响范围 / 解决期限 / 负责人：
验证证据：
替代的旧决定 / 被哪项决定替代：
```

## D013 — S00 最小可运行依赖与本地进程

- 状态：baseline；日期：2026-09-06；责任：当前任务。
- 决定：Node 24/Python 3.12、pnpm 10（packageManager 固定）、uv 0.8.19；Next.js 16/React 19，FastAPI/Celery 共享 Python 包。精确包版本只存锁文件。
- 原因：按 D006 仅引入当前代码使用的依赖，SQLAlchemy/Alembic、认证在 S01 接入，模型、解析、编辑器随后按阶段接入；S00 原生 CSS 足够支持状态页。
- 运行：Compose 管基础设施，开发脚本管理三个宿主进程；不在本阶段声称生产部署已验证。
- 健康：API 存活与就绪分开，Web 同源代理；真实 Worker 探针使用临时 RPC 结果回传。尚无业务任务提交端点，因此 D012 的持久化与入队补偿仍由 S02 落实。
- 初始化：显式幂等创建 vector 扩展与私有 bucket；不由 API 启动自动改表。
- 基线核对：[Next.js 16 运行要求](https://nextjs.org/docs/app/guides/upgrading/version-16)、[pnpm 10 安装](https://github.com/pnpm/pnpm.io/blob/main/versioned_docs/version-10.x/installation.md)、[MinIO 镜像与许可证](https://hub.docker.com/r/minio/minio/)。MinIO 固定已发布 release，仅本地开发；生产发布须重新验证维护与许可证条件。
- 验证证据与未验证项：见 STATUS S00 交接及 docs/S00_VALIDATION.md。

## D014 — S01 身份、授权和归档契约

- 状态：baseline；日期：2026-09-06。落实 D008 的开发机制与权限部分；生产 OIDC 供应商仍待配置。
- API：Bearer 身份；开发凭据为随机 token，数据库仅存 SHA-256 摘要，默认禁用且生产禁止启用。生产使用固定 RS256/JWKS、issuer、audience、exp、sub 验证，不信任请求提供的角色。
- 当前组织由请求选择，但必须查询有效 Membership；所有资源查询同时限定组织。owner 全组织；member/viewer 仅客户授权表列出的客户。owner 可创建客户，member 可修改已授权客户和创建/编辑项目，viewer 仅查看。
- 数据库：SQLAlchemy 2 + Alembic；组织一致性由组合外键加固。S01 不启用 RLS，应用层授权服务是 API 和未来 Worker 共用入口；未来任务执行必须重新验证成员及客户授权。
- 客户归档用 deleted_at，隐藏默认列表并禁止旗下项目修改/创建；归档项目保留可读且不可编辑。项目业务状态不能由 PATCH 任意修改，S01 仅 collecting 与 archive。
- 客户/项目增加 version 乐观锁；PATCH 和归档要求 version，旧版本返回 409。资源越权统一 404，角色不足 403；列表授权前置、UUID 游标分页。
- Web：同源代理，Bearer 存 HttpOnly/SameSite=Strict cookie；写请求检查 Origin。不会把服务端凭据写入公开前端变量。
- 验证：真实 PostgreSQL 两组织/两客户/三角色，伪造身份、跨组织/客户、归档和版本冲突测试；生产禁止开发模式测试。S02 起补充具体 Worker 用例的执行时鉴权测试。

D014 技术核对：[PyJWT 校验 API](https://pyjwt.readthedocs.io/en/stable/api.html)、[SQLAlchemy 组合外键](https://docs.sqlalchemy.org/en/20/core/constraints.html)。实现版本由锁文件维护；S01 验收见 docs/S01_VALIDATION.md。

## D017｜S05 澄清、对话与记忆

- 状态：implemented，本地验收通过；2026-09-09，当前任务。
- 澄清按八个必备类别生成固定问题，另提供风险建议问题；项目/类别唯一去重。明确回答直接形成带消息来源的待确认需求，复用 S04 合并、档案版本和显式冲突解决，不额外调用模型。检查缺口时同步已有回答状态，必答问题不能跳过。
- 新增 conversations/messages/generation_runs/clarifications/memories/audit_events。对话范围由项目推导客户和组织，避免重复存储；消息用 text/citation/tool_status/error 块，按会话 seq 分页，每页 100 条。
- 每项目仅一个活动对话运行；请求 UUID 保证同会话重试幂等。沿用 PostgreSQL outbox、180 秒租约、attempt fencing、最多三次进程恢复；模型调用不持有事务。开始与发布时重新鉴权，档案 version、会话 epoch 和已确认记忆版本变化时拒绝发布。
- SSE 使用运行内递增事件 ID，支持 Last-Event-ID / after；25 秒连接周期、逐秒心跳和实时凭据/权限重查。终态以数据库为准。事件存在 Run JSONB，当前完整 JSON 校验后发布分段文本，并非模型逐 token 实时输出；有低首字延迟需求时再做 provider streaming 和独立事件表。
- /resume 在 S05 创建幂等的后续运行，图检查点恢复由 S06 接入。waiting_user 释放 Worker，不占用活动运行槽。
- 会话记忆只用于该会话，项目记忆只用于该项目，客户记忆跨该客户的项目共享。保存默认 proposed，必须显式确认才进入模型上下文；删除、编辑、确认与重置记录最小审计元数据。
- 会话重置提升 epoch、停用会话记忆、取消旧运行，历史消息保留；项目记忆重置只停用 scope=project，客户记忆保留。两类重置先返回影响数量，confirm=true 才执行；并发变化时返回实际执行数量。
- 有界实现：每项目 100 会话、每客户 200 条未停用记忆；模型上下文最多最近 20 消息/20000 字及 20000 字确认记忆，档案最多 30000 字。无自动长期记忆推断、滚动摘要、工具执行或自动知识检索，S06 后按具体流程接入。

## D018｜S06 持久化人工确认

- 状态：implemented，验收见 [S06](docs/S06_VALIDATION.md)；收敛 D012 的工作流恢复部分。
- 使用 LangGraph 1.x 与官方 PostgreSQL saver 3.x，复用现有 generation_runs、dispatcher、Celery、SSE，不增加第二套任务系统。依赖精确版本见 uv.lock；LangChain/LangSmith 是传递依赖，本阶段不接追踪服务、不调用生成模型。
- 图节点是需求确认、大纲草拟、大纲确认。S04/S05 提取和澄清继续复用；授权检索在 Worker 准备输入时执行。状态保存服务端 scope、目标、需求 ID/version、Chunk ID/generation、固定大纲和警告。
- 原生 saver 使用业务事务的 psycopg 连接，sync durability；每次推进至中断或结束时 checkpoint、Run、消息、事件原子提交。中断不持有 Worker 或数据库连接；进程重启用 Run ID 作为 thread_id 读取检查点。纯节点允许重放。
- 每个确认门仅消费一次；resume 的 request UUID 和 gate 保存在 Run payload，重复请求返回原运行。每项目锁串行写入，保留 S05 queued/running 唯一约束，并限制一个 waiting/active 编排；等待时可用普通聊天补充需求。
- 恢复 API 和执行 Worker 分别重新鉴权、检查 epoch 与需求确认，确认大纲时比较档案版本和引用当前状态。归档/重置/取消沿用共享边界；失效输入要求取消并重开，不能确认旧大纲后悄悄替换输入。
- 原生 schema 由显式 Alembic 迁移创建，不在 Worker startup 自动 setup；新表索引用普通事务索引取代官方 setup 的 concurrent 创建。未来 saver 升级须显式迁移并跑回滚/重启测试。
- S06 固定 14 章大纲遵循阶段允许的受控节点；S07 接真实生成、可编辑大纲和版本。当前锁内仅本地检索与图操作；远程生成必须移出事务并校验发布版本。检查点保留期由部署阶段落实。

API 依据：[LangGraph interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts)、[Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)；本地安装的 PostgresSaver `_cursor` 已核对无业务事务自动提交。

## D019｜S07 方案生成、编辑与版本草案

- 状态：review；日期：2026-09-10，验收核对 2026-09-12；责任：当前任务。迁移、业务、接口和页面已落地，自动测试与真实链路证据见 [docs/S07_VALIDATION.md](docs/S07_VALIDATION.md)。浏览器与无障碍证据未在本次复现。
- 数据：每项目最多一个 `solutions`，`solution_versions` 保存完整不可变章节 JSONB 快照；最多 20 章、200 版本。每次大纲确认、单章生成/重生成或人工保存都创建新版本并复制未修改章节，不另建逐章表或独立 citation 表。
- 编辑：章节正文使用 Tiptap JSON，服务端限制每章序列化后最多 100000 字符、2000 节点、深度 12。白名单只允许段落、二/三级标题、无序/有序列表、引用、代码块、硬换行、横线和粗体/斜体/删除线/行内代码；StarterKit 不启用链接、下划线、图片或表格扩展，不接受 HTML。
- 引用：模型输出段落 text 及 source_id/quote；quote 必须逐字存在于服务端本次检索来源。正文写入编号 `[n]`，同章保存引用快照。`partial` 仅表示摘录匹配，不表示语义已验证；人工保存后整章引用降为 `unverified`。快照包含来源类型、文档/Chunk/generation 和定位，不以外键依赖会被重建或删除的 Chunk。
- 输入与大纲：创建必须引用 S06 `ready_for_generation` 的已完成 Run，服务端重验会话 epoch、需求档案及引用快照。先用通用 14 章模板建立方案，再由 `solution-v1` Prompt 生成定制大纲；用户可增删、重排后确认。保留既有 `section-xx` 或 UUID 稳定 ID；标题或目标变化会清除该章旧大纲引用。
- 生成：复用 GenerationRun、dispatcher、SSE 和现有 DeepSeek `request_json`，`generation.solution` 记录模型、usage 和 latency_ms。远程调用不持有项目事务；多章任务每章发布一个不可变版本并更新 remaining，失败保留已完成章节。attempt 按章推进，发布额外校验 step/baseVersion、会话 epoch、档案版本和引用状态，迟到结果不得覆盖人工编辑或新版本。
- 页面：从 S06 已完成编排进入独立方案页，先细化并确认大纲，再生成正文；支持单章生成/重生成、Tiptap 人工保存、未保存离开提示、运行恢复和历史只读查看。S06 原生图不承载远程模型事务。
- 边界：S08 的完整质量检查、Markdown/DOCX 导出及下载未实施；生产 OIDC 和完整无障碍审计仍待后续。验收需证明版本并发、单章保留、部分失败、引用失效、撤权及跨租户路径。

## D020｜S08 版本化质量检查与私有导出

- 状态：review；日期：2026-09-11，验收核对 2026-09-12；责任：当前任务。质量检查、Markdown/DOCX 导出、页面和即时鉴权下载已落地，自动与真实链路证据见 [docs/S08_VALIDATION.md](docs/S08_VALIDATION.md)。浏览器与无障碍证据未在本次复现。
- 任务与版本：复用 `generation_runs`、dispatcher、Worker 和既有 SSE/取消/租约边界，`payload.kind` 为 `verify` 或 `export`，不新增数据表。请求 UUID 幂等；每项绑定指定的不可变方案版本，质量结果另绑定需求档案版本，不会改写旧方案版本。
- 质量检查：将全部章节正文、本版引用及全部已确认需求发送给已配置的 DeepSeek JSON 适配器，上下文序列化后最多 100000 字符。模型必须返回所有需求的覆盖映射和带规则、严重程度、章节、说明、建议的问题；服务端校验需求/章节 ID，并追加必答澄清、引用当前 scope、章节完整度及未验证内容检查。
- 泄漏检查：本地规则只检测已登记的同组织其他客户名称在正文中的精确匹配。不向模型发送其他客户名称，不宣称检测别名、改写或其他私密数据；质量结果与导出全文都明确要求人工核实。
- 导出：MVP 仅支持 Markdown 和 DOCX。导出指定版本的章节顺序、受限 Tiptap 结构、章内引用编号、待核实标记及文末按章参考资料。DOCX 使用统一页面、标题、正文、页眉页脚样式，保留目录所需的标题级别；生成后重新检查 ZIP 并用 `python-docx` 打开，失败不发布。表格编辑与 Markdown/DOCX 表格保真已实现并有自动测试覆盖（≤50×20 矩形表，无合并单元格/自定义列宽）。
- 存储与下载：文件写入组织/项目私有对象路径，对象 key 包含 Run 和 attempt，避免迟到尝试覆盖已发布文件。只有当前成功 attempt 发布 key、SHA-256 和大小；迟到/取消/失败产物不可下载。API 每次下载重验成员、客户和项目 ACL，读取后核对 SHA-256，响应 `private, no-store` 并记录不含正文的审计元数据。因此 D010 的 S08 下载方案收敛为 API 鉴权代理，不使用签名 URL。
- 边界：不包含 PDF、品牌 DOCX 模板、完整 DLP 或自动认证。质量评分和问题是辅助审核，不能替代人工判断或把引用摘录当成事实支持。
