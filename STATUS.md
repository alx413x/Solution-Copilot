# Solution Copilot 项目状态

最后更新：2026-09-09。维护角色：总控任务。

## 当前快照

- 当前阶段：S04 需求结构化已完成，整合提交 `00cdedc`；下一阶段 S05 澄清与对话。
- 已完成：S00–S02 基础、客户项目与文档管线；S03 本地 Embedding、pgvector/FTS、授权前置混合检索、来源定位和调试页。
- 仓库事实：main 分支，S04 输入 `ebe5f68`，实现整合 `00cdedc`；当前文档提交归档最终状态，未推送远程。
- 验证情况：28 pytest、Ruff/Prettier、前端 TypeScript/生产构建、迁移一致性通过；真实 DeepSeek 合成提取 9 项/7.24 秒，浏览器预算冲突与人工编辑通过。见 [S04 验收记录](docs/S04_VALIDATION.md)。
- 环境情况：Node 24/Python 3.12、pnpm 10/uv 锁定；Docker 三项服务可用；BGE 固定 revision 已缓存至被忽略的 `.local`，DeepSeek 生成配置保存在被忽略的 `.env`。
- 当前阻塞：无 S04 本地阶段阻塞；模型痛点分类存在已记录偏差，需要人工核对。真实 OIDC、供应商生产数据策略及部署仍未验收。
- 当前执行任务：S04 完成；本次文档提交完成交接归档。
- 下一步：S05 接入消息、澄清回答与 SSE，沿用 S04 版本保护及来源快照。

## 阶段计划

状态取值：todo、in_progress、blocked、review、done。工作区内完成使用 review；整合版本和验收证据记录后才使用 done。各阶段默认以前一阶段验收产物为输入。

| ID / 建议任务名称      | 范围与产出                                          | 阶段验收门槛                                                      | 状态 / 负责人   |
| ---------------------- | --------------------------------------------------- | ----------------------------------------------------------------- | --------------- |
| S00 `01｜工程骨架`     | Web/API/Worker、依赖锁、Compose、配置示例、健康检查 | 一套记录下来的命令启动；Web 可调用 API，API/Worker 可连接基础设施 | done / 当前任务 |
| S01 `02｜客户与项目`   | 身份上下文、组织/成员/客户/项目、迁移、CRUD 页面    | 两组织样例验证越权被拒；客户和项目创建、编辑、归档可用            | done / 当前任务 |
| S02 `03｜文档处理`     | 上传、私有存储、解析、Chunk、任务状态、重试         | 四种格式文本提取和定位可验证；失败可重试，重复执行不重复入库      | done / 当前任务 |
| S03 `04｜知识检索`     | Embedding、向量/关键词索引、融合、检索调试          | 中文样例有评测记录；授权范围先过滤；引用可回到来源                | done / 当前任务 |
| S04 `05｜需求结构化`   | 提取 Schema、合并、冲突、需求编辑/确认              | 已确认数据不被覆盖；来源可定位；冲突需显式解决                    | done / 当前任务 |
| S05 `06｜澄清与对话`   | 问答、消息、Run/SSE、记忆、重置范围                 | 回答更新需求；断线恢复；会话重置不删除客户记忆                    | todo / 未分配   |
| S06 `07｜Agent 编排`   | 图节点、检查点、人工确认、恢复/取消                 | 澄清和大纲处暂停；进程重启可恢复；重复恢复不重复写入              | todo / 未分配   |
| S07 `08｜方案生成编辑` | 模板、大纲、分章生成、Tiptap、引用、版本            | 带引用方案可编辑；单章重生成保留其他章及旧版本                    | todo / 未分配   |
| S08 `09｜校验与导出`   | 质量问题定位、Markdown/DOCX、下载授权               | 导出可打开且引用一致；权限撤销符合既定下载策略                    | todo / 未分配   |
| S09 `10｜验收与交付`   | 脱敏数据、端到端评测、启动说明、已知限制            | 从创建客户到导出可复现；隔离、恢复和 AI 质量有测试证据            | todo / 未分配   |

S06 的大纲确认可先使用受控测试节点验证图；S07 接入真实生成，避免 S06 依赖尚未完成的 S07。

与 SPEC 第 23 节的映射：原阶段 0 = S00；原阶段 1 = S01–S03；原阶段 2 = S04–S05；原阶段 3 = S06–S07；原阶段 4 = S08–S09。SPEC 保留高层里程碑，本表负责具体执行状态。

## S00 交付与 S01 交接

- 阶段 / 负责人：S00 / 当前任务；状态 done。
- 输入快照：2026-09-05 六份项目规划文档；无初始 commit。
- 工作区 / 分支：`/Users/deyi/Documents/ChatGPT/SolutionEngineerAgent` / main。
- 交付：SPEC 4.7 全部清单与真实锁文件；Web 状态页、FastAPI 存活/就绪接口、Celery 队列探针、共享配置与基础设施适配器、Compose、私有 bucket/vector 显式初始化、README、测试和验收脚本。
- 验收证据：[docs/S00_VALIDATION.md](docs/S00_VALIDATION.md)；启动命令见 [README](README.md)。
- 决策：新增 D013（最小依赖、开发运行、健康检查）；D008–D012 仍按期限处理。
- 已知限制：无生产部署或业务功能；两项测试依赖弃用警告；其他平台与完整自动无障碍审计未验证。
- 整合版本：`0b40fec`（feat: initialize S00 Solution Copilot skeleton）；本次文档提交记录验收完成状态。
- 下一阶段第一步：读取 D008 与 SPEC 6–7、13–14，落实开发身份不可在生产启用及组织/客户授权矩阵；引入 SQLAlchemy/Alembic，完成两组织越权验证。
- 总控验收：S00 本地工程骨架门槛通过；S01 已在下方记录交付。

## S01 交付与 S02 交接

- 阶段 / 负责人：S01 / 当前任务；状态 done。
- 输入：61bdd0f（S00 已验收）；main，同一工作区。
- 交付范围：apps/api/auth.py、customers.py；共享 application/access.py、customers.py、schemas.py；domain/models.py、数据库配置；migrations；Web 登录/客户/项目页面及同源代理；seed、集成测试和 docs/S01_VALIDATION.md。
- 验收：12 pytest 通过、Alembic check 无差异、迁移往返、Web 会话与 S00 smoke、浏览器创建/编辑/归档和布局已验证；具体命令与结果见验收文档。
- 决策：D014 落实开发认证/授权/归档/版本控制；D008 生产供应商仍 open。SPEC 第 25 节记录实施契约。
- 未验证/限制：真实生产 OIDC；组织/成员/授权管理界面；RLS 未启用；具体业务 Worker 鉴权在 S02 实施；无生产部署/完整自动无障碍审计。
- 整合版本：`2203ad2`（feat: implement S01 customer and project management）；本次文档提交记录验收完成状态。
- S02 第一步：沿用 Identity 与客户/项目范围，执行任务时重新 resolve_identity；不能信任请求或队列中的 role。先定知识 scope、jobs/outbox 幂等及失败补偿，再接入私有文件上传和解析。
- 总控验收：S01 本地阶段门槛通过。

## S02 执行记录

- 2026-09-07：在 main、输入 `9a185bd` 开始；当前任务负责。已新增文档/分块/任务迁移、私有上传、四格式解析、durable dispatcher、API 与资料页，正在验证，尚未验收。
- 决策：D010/D012 的 S02 边界已收敛，新增 D015；索引检索与真实 token 计数留给 S03。
- 2026-09-08：23 项 pytest 通过（真实 PG 隔离 schema），Ruff/前端类型/生产构建通过；四格式真实 Web→API→MinIO→dispatcher→Celery→PG 冒烟通过，S00/S01 回归通过。
- 浏览器：上传 `s02-meeting.md`、解析/来源预览、重新解析/刷新终态、键盘焦点通过；320/768/1024/1440 无横向溢出、单 h1、控制台无 error/warn。演示文档 ID `1cac64ea-7a30-4d5f-800d-a54f3f453339` 保留供查看。
- 未覆盖/限制：无 OCR、复杂 PDF 表格结构保真、模型 token/embedding/检索、生产 OIDC/部署、完整 axe/屏幕阅读器审计；故障恢复以真实 PG 状态和故障注入验证，未进行整机崩溃测试。S07 前需补齐历史引用快照。
- 交接：迁移 head `9094c79bd5c7`；任务 generation + attempts + lease 持久化；保持 dispatcher 运行，S03 扩展索引步骤而非将 parsed 当成可检索。
- 验收证据：[docs/S02_VALIDATION.md](docs/S02_VALIDATION.md)。代码整合提交 `43589a3`，阶段验收通过；下一任务从该提交及最终文档归档版本继续。

## S03 执行记录

- 输入 `499f2ca`，main，当前任务；用户启用 ponytail，复用现有任务与数据库，未增加独立检索服务。
- 交付：向量/FTS 迁移、本地 BGE 512 维 Embedding、jieba/tokenizer 分块、授权前置双路召回与 RRF、检索调试界面、模型 profile 隔离及索引原子替换。
- 自动验收：25 项 PostgreSQL/pgvector/FTS 测试通过；十文档/三十问题真实 Web→Worker→模型→PG 评测 Hit@8 100%、MRR@8 0.901、热模型 HTTP P95 23.65 ms；前端 route types、TypeScript、生产构建通过。
- 浏览器：重建 `s02-meeting.md` 后显示“可检索”；中文查询首条命中并定位“业务需求 · 行 6–7”，来源链接和双路排名可查看，控制台无错误。
- 模型决定：S03 使用固定 revision 的本地 `BAAI/bge-small-zh-v1.5`，运行时不发送资料到外部服务；DeepSeek 配置留给 S04 生成调用验收。
- 已知边界：合成小语料、精确向量扫描、无 reranker/查询改写；OCR、历史引用快照、生产身份/部署及完整屏幕阅读器审计留待后续。
- 验收证据：[docs/S03_VALIDATION.md](docs/S03_VALIDATION.md) 与 `evals/s03-results.json`。实现提交 `656f0e6`，阶段验收通过；S04 从本次最终文档归档版本继续。

## S04 执行记录

- 输入 `ebe5f68`，main，当前任务，ponytail full。已完成需求档案、持久化提取任务、DeepSeek JSON 适配、引用校验、合并/冲突处理、人工编辑/确认 API 与页面。
- 新迁移 `1e32ee8d72cd` 已应用，Alembic check 无差异；28 pytest、Ruff/Prettier、TypeScript/生产构建通过。最终合成会议纪要经 API→Worker→DeepSeek 生成 9 项需求、引用逐字匹配，7.24 秒完成；八类完整度 87.5%，痛点合入背景的偏差已记录。
- 浏览器完成来源展开、100→80 万元预算冲突、显式采用新项及人工编辑保存；桌面排版与控制台正常。新页面四断点/完整无障碍审计未执行。证据见 [S04 验收记录](docs/S04_VALIDATION.md) 和 `evals/s04-results.json`。
- 决策见 D016。模型密钥仍只在忽略的 `.env`；证书校验通过 certifi 修复，未关闭 TLS 校验。
- 实现整合版本 `00cdedc`，本地阶段验收通过。S05 从该版本及最终文档提交继续，复用档案 version、来源快照与冲突规则，不绕过人工确认。

## 交接记录模板

```text
阶段 ID：
状态：
负责人 / 任务链接：
项目路径 / 工作区 / 分支：
输入版本（commit 或明确文件快照）：
目标及文件范围：
实际交付（文件、迁移、接口）：
验收证据（命令、结果、产物路径）：
未验证项 / 失败 / 已知限制：
新增或替代决策 ID：
整合版本（未提交时如实注明）：
下一阶段输入与第一步：
总控验收结果：
```

## 阻塞记录规则

阻塞必须包含可复现症状、影响阶段、已尝试方法、缺少的输入和恢复条件。未检查的配置属于“待验证”，不得直接登记为阻塞。任务失败不自动等同于阶段全部失败。

## 变更日志

| 日期       | 变更                                                 | 验证范围                                     |
| ---------- | ---------------------------------------------------- | -------------------------------------------- |
| 2026-09-05 | 基于参考讨论建立协同文档、S00–S09 执行计划与决策台账 | 文档结构、链接、范围及状态一致性；无运行验证 |
| 2026-09-06 | 完成 S00 骨架、真实依赖锁、运行链路与浏览器验收，更新 D013、架构和交接 | Ruff、3 pytest、类型/构建、队列/私有存储 smoke、Redis 故障恢复、响应式浏览器检查 |
| 2026-09-06 | S00 整合版本归档为 `0b40fec`，阶段由 review 转 done | 已有验收证据关联代码提交；暂存差异检查通过 |
| 2026-09-07 | S01 模型、迁移、授权 API 与页面已实现，验收中 | 初轮 7 pytest 与前端类型检查通过；生产构建/浏览器待验收 |
| 2026-09-07 | S01 完成工作区验收并整理交接 | 12 pytest、迁移一致性/往返、Web 会话及 S00 smoke、浏览器 CRUD/布局 |
| 2026-09-07 | S01 整合版本归档为 `2203ad2`，阶段由 review 转 done | 验收证据关联代码提交；提交内容不含本地凭据或缓存 |
| 2026-09-08 | S02 整合版本归档为 `43589a3`，阶段由 review 转 done | 23 pytest、四格式真实处理、浏览器来源与响应式验收 |
| 2026-09-09 | S03 本地模型、混合检索与来源链路完成验收 | 25 pytest、30 问实链路评测、前端构建与浏览器检索 |
| 2026-09-09 | S04 需求提取、确认保护与冲突解决完成本地验收 | 28 pytest、真实 DeepSeek、前端构建、浏览器冲突与编辑 |
